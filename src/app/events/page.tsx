import Events from '@/components/Events';

export default function EventsPage() {
    return (
        <main className="pt-24 pb-12 bg-bg-secondary min-h-screen">
            <Events />
        </main>
    );
}
